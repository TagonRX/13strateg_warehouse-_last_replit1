# 📦 Warehouse Management System (WMS)

**Система управления складом** для малого и среднего бизнеса с поддержкой штрихкодов, CSV импорта и полного цикла обработки заказов.

---

## 🎯 Основные возможности

### Инвентаризация
- ✅ Управление товарами (SKU, штрихкоды, размеры, цены)
- ✅ До 24 фотографий на товар
- ✅ Несколько штрихкодов на один товар
- ✅ Автоматическое извлечение локации из SKU (A101-F → A101)
- ✅ Архивирование товаров с нулевым остатком (4 дня grace period)
- ✅ Inline редактирование в таблице

### Сканирование штрихкодов
- ✅ USB сканеры (режим клавиатуры)
- ✅ Камера смартфона (требует HTTPS)
- ✅ WebSocket для удалённого сканирования
- ✅ Авто-фокус и зум для мобильных

### Импорт данных
- ✅ CSV импорт (до 2000 строк за раз)
- ✅ Интеллектуальное сопоставление товаров
- ✅ Разрешение конфликтов SKU
- ✅ Планировщик автоматического импорта (cron)
- ✅ История всех импортов

### Рабочие процессы

| Процесс | Описание |
|---------|----------|
| **Picking** | Создание и выполнение листов комплектации |
| **Testing** | Тестирование входящих товаров (New, Used, Exdisplay, Parts, Faulty) |
| **Placement** | Размещение товаров на складских локациях |
| **Dispatch** | 4-фазный процесс отправки с поддержкой этикеток |
| **Packing** | Мульти-воркер упаковка с статистикой |

### Аналитика
- ✅ Производительность работников
- ✅ Полный журнал событий (CSV экспорт)
- ✅ История импортов со статистикой
- ✅ Метрики по типам операций

---

## 🚀 Быстрый старт

### Вариант 1: PostgreSQL (рекомендуется)

```bash
# Распаковать архив
tar -xzf warehouse.tar.gz
cd warehouse

# Установить
./install.sh

# Запустить
./start.sh
```

Откройте браузер: **http://localhost:3000**  
Логин: `admin` / Пароль: `admin123`

### Вариант 2: SQLite (без PostgreSQL)

```bash
cd sqlite-version
./install.sh
./start.sh
```

Все данные в одном файле `warehouse.db`.

---

## 📁 Структура проекта

```
warehouse/
├── client/              # React фронтенд
│   └── src/
│       ├── components/  # UI компоненты
│       ├── pages/       # Страницы приложения
│       └── hooks/       # React хуки
├── server/              # Express бэкенд
│   ├── routes.ts        # API endpoints
│   ├── storage.ts       # Работа с БД
│   └── auth.ts          # Аутентификация
├── shared/              # Общие типы
│   └── schema.ts        # Drizzle схема БД
├── kubuntu-package/     # Готовый пакет для Kubuntu
└── sqlite-version/      # Версия на SQLite
```

---

## ⚙️ Конфигурация

Файл `kubuntu-config.js` содержит все настройки:

```javascript
module.exports = {
  // База данных PostgreSQL
  database: {
    host: 'localhost',
    port: 5432,
    name: 'warehouse_local',
    user: 'postgres'
  },
  
  // Веб-сервер
  server: {
    port: 3000,
    host: '0.0.0.0'
  },
  
  // Пути
  paths: {
    data: './data',
    backups: './backups',
    logs: './logs'
  }
};
```

---

## 🔧 Системные требования

### PostgreSQL версия
- Node.js 18+
- PostgreSQL 14+
- 2 GB RAM
- 10 GB свободного места

### Установка на Kubuntu (bash installer)

- Установщик: `kubuntu-package/install.sh` копирует проект в `/opt/warehouse`, собирает, настраивает ENV и systemd, переносит БД в `/var/lib/warehouse/warehouse.db`.

Шаги установки:

1) Клонируйте репозиторий и перейдите в каталог

```bash
git clone <repo-url>
cd 13strateg_warehouse-_last_replit1
```

2) Запустите установщик (требуются sudo права)

```bash
sudo bash kubuntu-package/install.sh
```

3) Проверьте статус сервиса и API

```bash
systemctl status warehouse.service
curl -s http://localhost:5000/api/debug/status | jq
```

4) Включите мониторинг IP (опционально)

```bash
sudo cp kubuntu-package/warehouse-ip-monitor.service /etc/systemd/system/
sudo cp kubuntu-package/warehouse-ip-monitor.timer /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable --now warehouse-ip-monitor.timer
```

Конфигурация:
- ENV файл: `/etc/warehouse/env` (по умолчанию `PORT=5000`, `SQLITE_PATH=/var/lib/warehouse/warehouse.db`, `NODE_ENV=production`)
- База данных: `/var/lib/warehouse/warehouse.db`
- Логи systemd: `journalctl -u warehouse.service -f`

Удаление:

```bash
sudo bash kubuntu-package/uninstall.sh
# Данные сохраняются по умолчанию
# Полный удаление:
sudo rm -rf /var/lib/warehouse /etc/warehouse /opt/warehouse
```

Смена порта:
- Измените `PORT` в `/etc/warehouse/env` и выполните `sudo systemctl restart warehouse.service`

Смена email для уведомлений IP:
- Измените переменную `MAIL_TO` в `kubuntu-package/ip-monitor.sh` и перезапустите таймер: `sudo systemctl restart warehouse-ip-monitor.timer`

### Nginx + HTTPS (Let’s Encrypt)

Настроить reverse proxy и TLS:

```bash
sudo bash kubuntu-package/setup-nginx.sh <domain> tagonte@gmail.com
# пример:
sudo bash kubuntu-package/setup-nginx.sh warehouse.local tagonte@gmail.com
```

Файлы конфигурации:
- `kubuntu-package/nginx-warehouse.conf` → устанавливается как `/etc/nginx/sites-available/warehouse.conf`
- Сайт активируется ссылкой в `/etc/nginx/sites-enabled/warehouse.conf`

Прокси перенаправляет на локальный сервис Express (`http://127.0.0.1:5000`). Сертификаты выпускать через certbot.

Автопродление сертификатов:
- Certbot устанавливает systemd‑таймер автоматически (`certbot.timer`). Проверить:

```bash
systemctl list-timers | grep certbot
sudo certbot renew --dry-run
```

Security‑хедеры:
- Включены базовые заголовки (HSTS, X‑Content‑Type‑Options, X‑Frame‑Options, Referrer‑Policy, CSP) в `kubuntu-package/nginx-warehouse.conf`.
- После изменения конфига перезапустите nginx: `sudo nginx -t && sudo systemctl restart nginx`.

### Обновление версии (upgrade)

Скрипт `kubuntu-package/upgrade.sh` обновляет установку в `/opt/warehouse` из локальной папки или Git‑репозитория:

```bash
# Из локальной папки с новой версией
sudo bash kubuntu-package/upgrade.sh /path/to/new/source

# Из Git‑репозитория (по умолчанию ветка main)
sudo bash kubuntu-package/upgrade.sh https://github.com/owner/repo.git main
```

Скрипт:
- Останавливает `warehouse.service`
- Копирует исходники (rsync или git clone)
- Выполняет `npm ci && npm run build`
- Проверяет `/etc/warehouse/env`
- Стартует сервис и запускает `smoke.sh`

Быстрый откат:
- Повторно запустите `upgrade.sh` с прошлой версией (например, из бэкапа или предыдущего Git‑тэга), либо `git checkout <tag>` в локальной папке и обновите из неё.
### SQLite версия
- Node.js 18+
- 1 GB RAM
- 5 GB свободного места

---

## 📊 База данных

### Основные таблицы

| Таблица | Описание |
|---------|----------|
| `users` | Пользователи (admin/worker) |
| `inventory_items` | Инвентарь (SKU, локации, цены) |
| `active_locations` | Локации склада |
| `orders` | Заказы (PENDING, DISPATCHED, PACKED) |
| `event_logs` | Журнал всех операций |
| `picking_lists` | Листы комплектации |

### Миграции

```bash
# Применить изменения схемы
npm run db:push

# Принудительно (при предупреждениях)
npm run db:push --force
```

---

## 🔐 Безопасность

- Bcrypt хеширование паролей (10 раундов)
- Сессионные токены (32 байта, hex)
- 24-часовой срок действия сессий
- Role-based доступ (admin/worker)

---

## 📝 API Endpoints

### Аутентификация
```
POST /api/auth/login        # Вход
POST /api/auth/logout       # Выход
GET  /api/auth/me           # Текущий пользователь
```

### Инвентарь
```
GET    /api/inventory       # Список товаров
POST   /api/inventory       # Добавить товар
PATCH  /api/inventory/:id   # Обновить товар
DELETE /api/inventory/:id   # Удалить товар
```

### Заказы
```
GET    /api/orders          # Список заказов
POST   /api/orders          # Создать заказ
PATCH  /api/orders/:id      # Обновить статус
```

---

## 🛠️ Разработка

```bash
# Установить зависимости
npm install

# Режим разработки (с hot reload)
npm run dev

# Сборка для production
npm run build

# Запустить production
npm start
```

---

## 📦 Создание архива для переноса

```bash
# Создать архив с данными
tar -czf warehouse-backup.tar.gz \
  --exclude=node_modules \
  --exclude=.git \
  warehouse/

# Экспорт данных БД
curl http://localhost:3000/api/admin/export-all > database-export.json
```

---

## 🐛 Решение проблем

### PostgreSQL не запускается
```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

### Ошибка подключения к БД
```bash
# Проверить настройки в kubuntu-config.js
# Убедиться что PostgreSQL trust authentication включен
sudo nano /etc/postgresql/14/main/pg_hba.conf
# Добавить: local all all trust
```

### Порт занят
```bash
# Найти процесс
lsof -i :3000

# Убить процесс
kill -9 <PID>
```

---

## 📄 Лицензия

MIT License - используйте свободно для любых целей.

---

## 👤 Автор

Разработано для оптимизации складских операций.

**Версия:** 2.0.0  
**Дата:** Декабрь 2024
