# Deployment Files for Warehouse Management System

Эта директория содержит все необходимые файлы для развертывания приложения на production сервере.

## Файлы

### 📋 DEPLOYMENT_GUIDE.md
Полное пошаговое руководство по развертыванию на Kubuntu/Ubuntu сервере.
- Детальные инструкции от начала до конца
- Все команды для копирования/вставки
- Решение типовых проблем
- Контрольный список готовности

**Начните с этого файла!**

### ⚙️ ecosystem.config.cjs
Конфигурация PM2 (Process Manager) для production.
- Кластерный режим для всех CPU
- Автоматический перезапуск
- Логирование
- Graceful shutdown

**Использование:**
```bash
pm2 start ecosystem.config.cjs --env production
```

### 🌐 nginx.conf
Конфигурация Nginx как reverse proxy.
- Поддержка WebSocket для barcode scanner
- SSL готов (закомментирован)
- Сжатие и кэширование
- Настройки безопасности

**Установка:**
```bash
sudo cp nginx.conf /etc/nginx/sites-available/warehouse
sudo ln -s /etc/nginx/sites-available/warehouse /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### 🔐 .env.production.example
Пример production переменных окружения.
- DATABASE_URL
- SESSION_SECRET
- Другие настройки

**Использование:**
```bash
cp deployment/.env.production.example .env
# Затем отредактировать .env своими значениями
nano .env
```

### 🚀 setup.sh
Автоматический скрипт полной установки.
- Устанавливает все зависимости
- Настраивает PostgreSQL
- Конфигурирует Nginx
- Запускает приложение

**Использование:**
```bash
chmod +x deployment/setup.sh
./deployment/setup.sh
```

⚠️ **Важно:** Скрипт запросит:
- Имя базы данных
- Имя пользователя БД
- Пароль БД
- Домен/IP адрес

### 💾 backup.sh
Скрипт резервного копирования базы данных.
- Создает сжатый дамп БД
- Автоматически удаляет старые копии (>7 дней)
- Поддержка отправки в облако

**Использование:**
```bash
chmod +x deployment/backup.sh
./deployment/backup.sh
```

**Автоматическое резервное копирование:**
```bash
crontab -e
# Добавить строку для ежедневного бэкапа в 2 AM:
0 2 * * * /var/www/warehouse/deployment/backup.sh
```

### 🔄 restore.sh
Скрипт восстановления из резервной копии.
- Восстанавливает БД из дампа
- Безопасно останавливает/запускает приложение

**Использование:**
```bash
chmod +x deployment/restore.sh
./deployment/restore.sh backups/warehouse_backup_20250125_120000.sql.gz
```

---

## Быстрый старт

### Вариант 1: Автоматическая установка (рекомендуется)

```bash
# 1. Загрузить проект на сервер
cd /var/www
sudo mkdir warehouse
sudo chown $USER:$USER warehouse
cd warehouse

# 2. Скопировать файлы проекта сюда

# 3. Запустить установку
chmod +x deployment/setup.sh
./deployment/setup.sh
```

### Вариант 2: Ручная установка

Следуйте инструкциям в `DEPLOYMENT_GUIDE.md`.

---

## Структура после развертывания

```
/var/www/warehouse/
├── client/              # Frontend исходники
├── server/              # Backend исходники
├── dist/                # Скомпилированное приложение
├── deployment/          # Эта директория
├── logs/                # PM2 логи
├── backups/             # Резервные копии БД
├── node_modules/        # NPM зависимости
├── .env                 # Production переменные окружения
├── package.json         # NPM конфигурация
└── ecosystem.config.cjs  # Симлинк на deployment/ecosystem.config.cjs
```

---

## Проверка работоспособности

После развертывания проверьте:

```bash
# Статус приложения
pm2 status

# Логи приложения
pm2 logs warehouse --lines 50

# Статус Nginx
sudo systemctl status nginx

# Статус PostgreSQL
sudo systemctl status postgresql

# Открыть в браузере
# http://ваш-ip-адрес или http://ваш-домен.com
```

**Первый вход:**
- Логин: `admin`
- Пароль: `admin123`

⚠️ **Измените пароль сразу после входа!**

---

## Обслуживание

### Просмотр логов
```bash
pm2 logs warehouse          # Все логи
pm2 logs warehouse --err    # Только ошибки
tail -f logs/pm2-error.log  # Файл ошибок
```

### Перезапуск приложения
```bash
pm2 restart warehouse       # Перезапуск
pm2 reload warehouse        # Zero-downtime перезапуск
pm2 stop warehouse          # Остановка
pm2 start warehouse         # Запуск
```

### Обновление приложения
```bash
cd /var/www/warehouse
pm2 stop warehouse
git pull origin main        # или загрузить новые файлы
npm install --production
npm run build
npm run db:push
pm2 start warehouse
```

### Резервное копирование
```bash
# Вручную
./deployment/backup.sh

# Список резервных копий
ls -lh backups/

# Восстановление
./deployment/restore.sh backups/warehouse_backup_YYYYMMDD_HHMMSS.sql.gz
```

---

## Решение проблем

### Приложение не запускается
```bash
pm2 logs warehouse --lines 200
pm2 describe warehouse
```

### Ошибка подключения к БД
```bash
# Проверить PostgreSQL
sudo systemctl status postgresql

# Проверить .env
cat .env | grep DATABASE_URL

# Тест подключения
psql -U warehouse_user -d warehouse_db -h localhost -W
```

### Nginx 502 Bad Gateway
```bash
# Проверить, что приложение запущено
pm2 status

# Логи Nginx
sudo tail -f /var/log/nginx/warehouse_error.log
```

### WebSocket не работает
```bash
# Проверить Nginx конфигурацию для /ws
sudo nano /etc/nginx/sites-available/warehouse

# Перезапустить Nginx
sudo systemctl restart nginx
```

---

## Безопасность

### SSL сертификат
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.com
```

### Изменить пароль администратора
1. Войти в систему
2. Admin Panel → Users
3. Изменить пароль

### Автоматические обновления
```bash
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades
```

---

## Поддержка

Если возникли проблемы:
1. Проверьте `pm2 logs warehouse`
2. Проверьте статус всех сервисов
3. Убедитесь, что .env настроен правильно
4. Проверьте DEPLOYMENT_GUIDE.md раздел "Решение проблем"

---

**Успешного развертывания! 🚀**
