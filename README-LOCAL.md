# Warehouse Management System - Локальная установка

Руководство по установке и запуску системы управления складом на локальной машине **Kubuntu 25.10**.

## 📋 Требования

- **ОС**: Kubuntu 25.10 (или Ubuntu-based дистрибутив)
- **Процессор**: 2+ ядра
- **RAM**: Минимум 4GB
- **Диск**: Минимум 2GB свободного места
- **Интернет**: Для установки зависимостей

## 🚀 Быстрый старт

### 1. Первая установка

Запустите скрипт установки из папки проекта:

```bash
cd /home/ruslan/Desktop/Main-project-13strategyexperts
chmod +x setup.sh
./setup.sh
```

Скрипт автоматически:
- ✅ Проверит и установит Node.js 20 (через nvm)
- ✅ Проверит и установит PostgreSQL
- ✅ Создаст базу данных `warehouse_local`
- ✅ Сгенерирует `.env` файл с настройками
- ✅ Установит все npm пакеты
- ✅ Применит схему БД
- ✅ Создаст admin пользователя (логин: `admin`, пароль: `admin123`)

### 2. Запуск для тестирования

После установки запустите вручную:

```bash
chmod +x start.sh
./start.sh
```

Откройте браузер: **http://localhost:5000**

### 3. Остановка сервера

```bash
chmod +x stop.sh
./stop.sh
```

### 4. Автозапуск при старте системы

Установите systemd сервис:

```bash
sudo cp warehouse.service /etc/systemd/system/
sudo systemctl daemon-reload
sudo systemctl enable warehouse
sudo systemctl start warehouse
```

Проверка статуса:

```bash
sudo systemctl status warehouse
```

## 📂 Структура файлов

```
/home/ruslan/Desktop/Main-project-13strategyexperts/
├── setup.sh              # Первая установка
├── start.sh              # Ручной запуск
├── stop.sh               # Остановка
├── collect-logs.sh       # Сбор логов для отладки
├── warehouse.service     # Systemd сервис
├── .env                  # Настройки (создается автоматически)
├── package.json
├── server/
├── client/
└── shared/
```

## 🔧 Управление

### Управление через systemd (рекомендуется)

```bash
# Запуск
sudo systemctl start warehouse

# Остановка
sudo systemctl stop warehouse

# Перезапуск
sudo systemctl restart warehouse

# Статус
sudo systemctl status warehouse

# Логи
sudo journalctl -u warehouse -f
```

### Ручное управление

```bash
# Запуск
./start.sh

# Остановка
./stop.sh
```

## 📊 Логи

### Расположение логов

- **Приложение**: `/var/log/warehouse/stdout.log` и `/var/log/warehouse/stderr.log`
- **Systemd**: `journalctl -u warehouse`
- **PostgreSQL**: `/var/log/postgresql/`
- **Ручной запуск**: `/var/log/warehouse/manual_run.log`

### Сбор всех логов для отладки

```bash
chmod +x collect-logs.sh
./collect-logs.sh
```

Создаст архив `warehouse-logs-YYYY-MM-DD-HHMMSS.tar.gz` с:
- Системной информацией
- Логами приложения
- Логами PostgreSQL
- Логами systemd
- Информацией о БД
- Конфигурационными файлами

Отправьте этот архив для диагностики проблем.

## 🗄️ База данных

### Подключение к БД

```bash
# Загрузить переменные из .env
source .env

# Подключиться к БД
psql "$DATABASE_URL"
```

### Полезные команды PostgreSQL

```sql
-- Список таблиц
\dt

-- Информация о таблице
\d+ users

-- Количество записей
SELECT COUNT(*) FROM inventory;

-- Размер БД
SELECT pg_size_pretty(pg_database_size('warehouse_local'));
```

### Резервное копирование БД

```bash
source .env
pg_dump "$DATABASE_URL" > warehouse_backup_$(date +%Y%m%d).sql
```

### Восстановление БД

```bash
source .env
psql "$DATABASE_URL" < warehouse_backup_20251108.sql
```

## 🔐 Безопасность

### Изменение пароля admin

1. Войдите в систему как admin
2. Перейдите в "Пользователи"
3. Измените пароль

### Изменение SESSION_SECRET

```bash
# Сгенерировать новый секрет
openssl rand -base64 32

# Обновить в .env
nano .env
# Найти SESSION_SECRET и заменить значение

# Перезапустить
sudo systemctl restart warehouse
```

## ⚙️ Настройки (.env)

Основные переменные в `.env`:

```bash
# База данных
DATABASE_URL=postgresql://warehouse_user:password@localhost:5432/warehouse_local
PGHOST=localhost
PGPORT=5432
PGDATABASE=warehouse_local
PGUSER=warehouse_user
PGPASSWORD=password

# Секрет сессии
SESSION_SECRET=your-random-secret-here

# Окружение
NODE_ENV=production
```

## 🔍 Диагностика проблем

### Проблема: Порт 5000 занят

```bash
# Проверить что использует порт
sudo lsof -i :5000

# Остановить процесс
sudo kill $(sudo lsof -Pi :5000 -sTCP:LISTEN -t)
```

### Проблема: PostgreSQL не запускается

```bash
# Проверить статус
sudo systemctl status postgresql

# Проверить логи
sudo journalctl -u postgresql -n 50

# Перезапустить
sudo systemctl restart postgresql
```

### Проблема: Ошибки при npm install

```bash
# Очистить кэш
npm cache clean --force

# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install
```

### Проблема: База данных не создается

```bash
# Создать вручную
sudo -u postgres psql
CREATE DATABASE warehouse_local;
CREATE USER warehouse_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE warehouse_local TO warehouse_user;
\q
```

## 🔄 Обновление приложения

### Из Replit

```bash
# Остановить сервис
sudo systemctl stop warehouse

# Перейти в папку проекта
cd /home/ruslan/Desktop/Main-project-13strategyexperts

# Скачать последнюю версию (git или скачать ZIP)
# Вариант 1: Git
git pull origin main

# Вариант 2: Замена файлов вручную
# Скачайте ZIP с Replit и замените файлы

# Установить зависимости
npm install

# Применить изменения БД
npm run db:push

# Запустить
sudo systemctl start warehouse
```

## 📱 Доступ с других устройств

### Локальная сеть

Приложение доступно по IP адресу компьютера:

```bash
# Узнать IP адрес
hostname -I

# Пример: http://192.168.1.100:5000
```

### Настройка firewall (если нужно)

```bash
# Разрешить порт 5000
sudo ufw allow 5000/tcp

# Проверить статус
sudo ufw status
```

## 🛠️ Продвинутые настройки

### Изменение порта

По умолчанию используется порт **5000**. Для изменения:

1. Отредактируйте `server/index.ts`
2. Найдите строку с `const PORT = 5000`
3. Измените на нужный порт
4. Обновите `warehouse.service` если нужно
5. Перезапустите: `sudo systemctl restart warehouse`

### Автоматическое резервное копирование

Создайте cron задачу:

```bash
# Открыть crontab
crontab -e

# Добавить задачу (каждый день в 2:00)
0 2 * * * cd /home/ruslan/Desktop/Main-project-13strategyexperts && source .env && pg_dump "$DATABASE_URL" > ~/backups/warehouse_$(date +\%Y\%m\%d).sql
```

## 📞 Поддержка

При возникновении проблем:

1. Соберите логи: `./collect-logs.sh`
2. Отправьте архив `warehouse-logs-*.tar.gz` для диагностики
3. Опишите проблему и шаги для воспроизведения

## ✅ Чек-лист после установки

- [ ] Приложение открывается в браузере (http://localhost:5000)
- [ ] Вход под admin/admin123 работает
- [ ] Можно создать товар в Stock-In
- [ ] PostgreSQL запущен и работает
- [ ] Логи пишутся в `/var/log/warehouse/`
- [ ] Systemd сервис включен и работает
- [ ] Изменен пароль admin на безопасный
- [ ] Настроено резервное копирование (опционально)

---

**Версия документа**: 1.0  
**Дата**: 8 ноября 2025  
**Проект**: Warehouse Management System
