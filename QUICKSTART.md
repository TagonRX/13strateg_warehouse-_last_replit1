# 🏭 Warehouse Management System - SQLite Edition

Проект переведён на **SQLite** для локальной разработки на Kubuntu без необходимости установки PostgreSQL/Neon.

## 🚀 Быстрый старт

### Способ 1: Использовать скрипт (рекомендуется)

```bash
chmod +x ./start.sh
./start.sh
```

Скрипт автоматически:
- Проверит Node.js и npm
- Создаст `.env` если его нет
- Установит зависимости если нужно
- Запустит dev сервер на порту 5000

### Способ 2: Ручной запуск

```bash
# 1. Установить зависимости (если еще не установлены)
npm install

# 2. Запустить dev сервер
npm run dev
```

## 📋 Требования

- **Node.js** 18+ (проверка: `node --version`)
- **npm** (проверка: `npm --version`)

## 🌐 Доступ к приложению

После запуска сервер будет доступен:
- **Фронтенд**: http://localhost:5173 (Vite)
- **API сервер**: http://localhost:5000

## 📁 База данных

- **Тип**: SQLite
- **Файл**: `warehouse.db` (создаётся автоматически в корне проекта)
- **Место**: локальный файл (не требует отдельного сервера БД)

## 🔧 Доступные команды

```bash
# Запуск dev сервера (с hot-reload)
npm run dev

# Сборка для production
npm build

# Запуск production сборки
npm start

# Проверка типов TypeScript
npm run check

# Обновить схему БД (для миграций)
npm run db:push
```

## 💾 Миграции БД

Таблицы создаются автоматически при первом запуске. Если нужно пересоздать БД:

```bash
# 1. Удалить текущую БД
rm warehouse.db

# 2. Запустить сервер (таблицы создадутся заново)
npm run dev
```

## 📝 Переменные окружения

Файл `.env` содержит:
```
NODE_ENV=development
VITE_SUPABASE_ANON_KEY=<key>
```

## 🐛 Решение проблем

**Ошибка: "SQLITE_ERROR: no such table"**
- Первый запуск - дождитесь создания таблиц (5-10 секунд)
- Повторное запуск: `rm warehouse.db && npm run dev`

**Ошибка: "EADDRINUSE" (порт занят)**
```bash
# Найти процесс на порту 5000
lsof -i :5000

# Завершить процесс
kill -9 <PID>

# Или использовать другой порт
PORT=3000 npm run dev
```

**Медленно запускается в первый раз**
- Нормально - происходит инициализация БД и создание таблиц
- Последующие запуски будут быстрее

## 📚 Структура проекта

```
├── client/              # Фронтенд (React + Vite)
├── server/              # Бэкенд (Express + TypeScript)
├── shared/              # Общие типы и схемы
├── warehouse.db         # SQLite БД (создаётся при запуске)
├── start.sh             # Скрипт быстрого запуска
└── package.json         # Зависимости проекта
```

## 🎯 Для разработки на Kubuntu

После первого `npm run dev`:
1. Откройте http://localhost:5173 в браузере
2. БД уже создана и готова к использованию
3. Все изменения в коде будут применены автоматически

**Совет**: Используйте `./start.sh` для повторного запуска - скрипт позаботится обо всех деталях.

---

**Контакты**: Проект разработан для локальной разработки на Kubuntu Linux.
