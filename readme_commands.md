# Команды для управления сервером

## 🚀 Запуск сервера

### Через PM2 (рекомендуется - автозапуск)
```bash
pm2 start warehouse
# или если еще не настроен:
pm2 start npm --name "warehouse" -- run dev
pm2 save
```

### Режим разработки (development) - вручную
```bash
npm run dev
```
Запускает одновременно сервер (port 5000) и клиент (port 5173)

### Только сервер
```bash
npm run server
```

### Только клиент
```bash
npm run client
```

### Production режим
```bash
npm run build
npm start
```

---

## 📦 Управление зависимостями

### Установка всех зависимостей
```bash
npm install
```

### Обновление зависимостей
```bash
npm update
```

### Проверка устаревших пакетов
```bash
npm outdated
```

---

## 🔄 Работа с Git/GitHub

### Получить обновления с GitHub
```bash
git pull origin main
```

### Проверить статус изменений
```bash
git status
```

### Отправить изменения на GitHub
```bash
# 1. Добавить файлы
git add .

# 2. Создать коммит
git commit -m "описание изменений"

# 3. Отправить на GitHub
git push origin main
```

### Посмотреть историю коммитов
```bash
git log --oneline
```

### Отменить локальные изменения
```bash
git restore .
```

---

## 🗄️ Работа с базой данных

### Применить изменения схемы в БД
```bash
npm run db:push
```

### Сброс базы данных (удалить и пересоздать)
```bash
rm warehouse.db warehouse.db-shm warehouse.db-wal
npm run dev
```

---

## 🔍 Проверка и отладка

### Проверка типов TypeScript
```bash
npm run check
```

### Проверка версий
```bash
node --version
npm --version
git --version
```

### Просмотр логов в реальном времени
```bash
# Если используете PM2
pm2 logs warehouse
```

---

## 🛠️ Управление PM2

### Основные команды
```bash
pm2 list              # Список всех процессов
pm2 logs warehouse    # Просмотр логов
pm2 restart warehouse # Перезапустить сервер
pm2 stop warehouse    # Остановить сервер
pm2 start warehouse   # Запустить сервер
pm2 delete warehouse  # Удалить из PM2
```

### Мониторинг
```bash
pm2 monit            # Интерактивный мониторинг
pm2 show warehouse   # Детальная информация
```

### Автозапуск
```bash
pm2 save            # Сохранить текущую конфигурацию
crontab -l          # Проверить настройки автозапуска
```

**Примечание:** Автозапуск настроен через crontab. Сервер автоматически запустится через 30 секунд после перезагрузки системы.

---

## 🛠️ Полезные команды

### Остановить сервер
```
Ctrl + C (в терминале где запущен сервер)
```

### Очистка кэша npm
```bash
npm cache clean --force
```

### Удалить node_modules и переустановить
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проверить занятые порты
```bash
# Проверить порт 5000
lsof -i :5000

# Проверить порт 5173
lsof -i :5173
```

### Убить процесс на порту
```bash
# Например, порт 5000
kill -9 $(lsof -t -i:5000)
```

---

## 📝 Данные для входа по умолчанию

- **Логин:** admin
- **Пароль:** 123456

---

## 🌐 URL адреса

- **Frontend:** http://localhost:5173
- **Backend API:** http://localhost:5000
- **Network:** http://192.168.0.118:5173 (для доступа с других устройств в сети)

---

## 🔄 Полный цикл обновления проекта

```bash
# 1. Остановить сервер
pm2 stop warehouse

# 2. Получить обновления
git pull origin main

# 3. Обновить зависимости (если нужно)
npm install

# 4. Применить изменения БД (если нужно)
npm run db:push

# 5. Запустить сервер
pm2 restart warehouse
```

---

## 🔄 После перезагрузки компьютера

### Автоматический запуск
Сервер запустится **автоматически** через 30 секунд после загрузки системы.

Проверить статус:
```bash
pm2 list
pm2 logs warehouse --lines 50
```

### Если нужно запустить вручную
```bash
cd ~/Desktop/13strateg_warehouse-_last_replit1
pm2 start warehouse
```

---

## 🐛 Устранение проблем

### Сервер не запускается
```bash
# Проверить, свободны ли порты
lsof -i :5000
lsof -i :5173

# Убить процессы на портах
kill -9 $(lsof -t -i:5000)
kill -9 $(lsof -t -i:5173)

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install
```

### Ошибки базы данных
```bash
# Пересоздать БД
rm warehouse.db warehouse.db-shm warehouse.db-wal
npm run dev
```

### Git конфликты
```bash
# Сохранить локальные изменения
git stash

# Получить обновления
git pull origin main

# Восстановить изменения
git stash pop
```

---

## 📚 NVM (Node Version Manager)

### Установка Node.js через nvm
```bash
nvm install 20
nvm use 20
```

### Проверка установленных версий
```bash
nvm list
```

### Использовать определенную версию
```bash
nvm use 20
```
