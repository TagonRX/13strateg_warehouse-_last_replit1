# 📸 Система управления изображениями товаров

## Обзор

Автоматическая система скачивания и хранения изображений товаров с интеллектуальным кэшированием.

---

## 🎯 Основные возможности

### 1. **Автоматическое скачивание**
- При добавлении нового SKU с `quantity > 0` - фото скачивается автоматически
- При обновлении `quantity` с 0 на > 0 - фото загружается заново
- Работает в фоновом режиме, не блокирует интерфейс

### 2. **Автоматическое удаление**
- Когда `quantity` товара становится 0 - фото удаляется
- Освобождает место на диске
- Поддерживает актуальность данных

### 3. **Умное кэширование**
- Локальные копии изображений хранятся в `server/public/images/products/`
- Повторное скачивание не происходит если файл уже существует
- Автоматическая проверка локального кэша при загрузке страницы

### 4. **Fallback на оригинальные URL**
- Если локальная копия отсутствует - показывается оригинальное изображение (eBay)
- При ошибке загрузки - отображается плейсхолдер
- Плавная деградация без поломки интерфейса

---

## 📂 Структура файлов

```
server/
  public/
    images/
      products/
        SKU_NAME.jpg         # Локальные копии изображений
        SKU_NAME.png
  image-manager.ts           # Утилиты управления изображениями
  routes.ts                  # API endpoints

client/
  src/
    components/
      SmartImage.tsx         # Умный компонент изображения
      InventoryTable.tsx     # Использование SmartImage
```

---

## 🔧 API Endpoints

### 1. **POST /api/inventory/download-image**
Скачивает изображение и сохраняет локально.

**Request:**
```json
{
  "sku": "A101-F",
  "imageUrl": "https://i.ebayimg.com/..."
}
```

**Response:**
```json
{
  "success": true,
  "localPath": "/images/products/A101-F.jpg"
}
```

### 2. **GET /api/inventory/image/:sku**
Проверяет наличие локальной копии изображения.

**Response:**
```json
{
  "exists": true,
  "localPath": "/images/products/A101-F.jpg"
}
```

### 3. **GET /images/products/:filename**
Раздача статических файлов (автоматически через Express static).

---

## ⚙️ Автоматические процессы

### При создании нового товара (CSV Import / API)
```typescript
if (itemData.imageUrl && itemData.quantity > 0) {
  downloadImage(itemData.imageUrl, itemData.sku); // Фон
}
```

### При обновлении количества
```typescript
// Удаление при quantity = 0
if (updates.quantity === 0 && existing.quantity > 0) {
  await deleteImage(existing.sku);
}

// Загрузка при quantity > 0 (из 0)
if (updates.quantity > 0 && existing.quantity === 0) {
  downloadImage(imageUrl, existing.sku); // Фон
}
```

---

## 🧩 Компонент SmartImage

### Возможности
1. **Проверка локального кэша** при монтировании
2. **Фоновая загрузка** если кэша нет
3. **Автоматический переход** с eBay URL на локальный
4. **Lazy loading** для оптимизации
5. **Обработка ошибок** с плейсхолдером

### Использование
```tsx
<SmartImage
  sku="A101-F"
  imageUrl="https://i.ebayimg.com/..."
  alt="Product name"
  className="w-12 h-12 object-cover"
/>
```

### Жизненный цикл
1. Компонент монтируется
2. Запрос к `/api/inventory/image/:sku`
3. Если локальный файл есть → показывается сразу
4. Если нет → показывается оригинал + запускается фоновая загрузка
5. После загрузки → автоматически переключается на локальный файл
6. При ошибке → показывается плейсхолдер

---

## 🗑️ Очистка неиспользуемых изображений

### Ручная очистка
```typescript
import { cleanupOrphanedImages } from './image-manager';

// Получить все активные SKU
const activeSkus = new Set(['SKU1', 'SKU2', ...]);

// Удалить неактивные изображения
const deletedCount = await cleanupOrphanedImages(activeSkus);
console.log(`Deleted ${deletedCount} orphaned images`);
```

### Автоматическая очистка
Происходит автоматически при `quantity = 0`.

---

## 📊 Формат имен файлов

### Генерация имени
```typescript
// SKU → безопасное имя файла
const sanitizedSku = sku.replace(/[^a-zA-Z0-9-_]/g, '_');
// "A101-F" → "A101-F.jpg"
// "A B C" → "A_B_C.jpg"
```

### Поддерживаемые форматы
- `.jpg` / `.jpeg`
- `.png`
- `.webp`
- `.gif`

---

## 🔒 Безопасность

### User-Agent
Используется реальный User-Agent браузера для обхода блокировок:
```typescript
headers: {
  'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
}
```

### Валидация путей
- Все имена файлов санитизируются
- Используется `path.join()` для предотвращения path traversal
- Проверка расширений файлов

---

## 📈 Оптимизация производительности

### 1. **Lazy Loading**
Изображения загружаются только при попадании в viewport:
```tsx
<img loading="lazy" ... />
```

### 2. **Фоновое скачивание**
Не блокирует UI, работает асинхронно:
```typescript
downloadImage(...).catch(err => console.error(err));
```

### 3. **Кэширование**
- Повторное скачивание не происходит
- Проверка существования перед загрузкой
- Использование локальной файловой системы

### 4. **Статическая раздача**
Express Static Middleware для быстрой отдачи файлов:
```typescript
app.use('/images', express.static('server/public/images'));
```

---

## 🐛 Обработка ошибок

### Сценарии
1. **Сеть недоступна** → плейсхолдер
2. **URL невалиден** → плейсхолдер
3. **Формат не поддерживается** → плейсхолдер
4. **CORS блокировка** → фоновая загрузка на сервере
5. **Файл не найден** → попытка загрузки

### Логирование
```typescript
console.error(`Failed to download image for ${sku}:`, error);
```

---

## 💡 Примеры использования

### Пример 1: Добавление товара через CSV
```csv
itemId,sku,name,quantity,imageUrl
12345,A101-F,Test Product,10,https://i.ebayimg.com/images/g/xyz.jpg
```
→ Фото автоматически скачается при импорте

### Пример 2: Обновление количества
```typescript
// Было: quantity = 5
// Стало: quantity = 0
→ Фото удалится автоматически

// Было: quantity = 0
// Стало: quantity = 10
→ Фото загрузится автоматически
```

### Пример 3: Ручная загрузка
```typescript
const result = await fetch('/api/inventory/download-image', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    sku: 'A101-F',
    imageUrl: 'https://...'
  })
});
```

---

## 📝 Логи и мониторинг

### Сервер
```bash
pm2 logs warehouse
```

### Ключевые сообщения
```
Auto-deleted image for SKU A101-F (quantity = 0)
Failed to download image for A101-F: Network error
Deleted image: A101-F.jpg
```

---

## 🔮 Будущие улучшения

- [ ] Автоматическая компрессия изображений
- [ ] Генерация thumbnails разных размеров
- [ ] Периодическая проверка битых изображений
- [ ] Dashboard с статистикой кэша
- [ ] CDN интеграция для масштабирования
- [ ] WebP конвертация для экономии места

---

## 📞 Поддержка

При проблемах проверьте:
1. Права доступа к папке `server/public/images/products/`
2. Наличие свободного места на диске
3. Логи PM2: `pm2 logs warehouse`
4. Консоль браузера (F12)

**Папка с изображениями:** `/home/rusan/Desktop/13strateg_warehouse-_last_replit1/server/public/images/products/`
