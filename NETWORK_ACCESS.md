# Доступ к серверу из локальной сети

## ✅ Сервер настроен и работает!

### 📡 Адреса доступа:

**Frontend (Vite):**
- Локально: http://localhost:5173
- Из сети: http://10.24.160.26:5173

**Backend API (Express):**
- Локально: http://localhost:5000
- Из сети: http://10.24.160.26:5000

### 🔧 Конфигурация:

1. **Vite** настроен на `host: 0.0.0.0` - слушает все сетевые интерфейсы
2. **Express** работает на порту 5000
3. **Оба сервера** запускаются одновременно через `npm run dev`

### 🌐 Доступ из других компьютеров в сети:

Любой компьютер в вашей локальной сети может открыть:
- **http://10.24.160.26:5173** - для работы с приложением
- **http://10.24.160.26:5000** - для прямого обращения к API

### 🔐 Учетные данные:

- **Логин:** admin
- **Пароль:** 123456

### 🚀 Запуск сервера:

```bash
npm run dev
```

Эта команда запускает:
- Backend сервер на порту 5000
- Frontend сервер на порту 5173

### 📝 Примечания:

1. **Firewall:** Убедитесь, что порты 5000 и 5173 открыты в файрволле вашего компьютера
2. **IP адрес:** 10.24.160.26 - это IP вашего компьютера в локальной сети
3. **Dev Container:** Если вы работаете в Dev Container, VS Code автоматически пробросит порты на хост

### 🔥 Проверка файрволла (если не работает доступ):

```bash
# Проверить статус UFW
sudo ufw status

# Разрешить порты (если нужно)
sudo ufw allow 5173/tcp
sudo ufw allow 5000/tcp
```

### 🧪 Тест доступа:

С другого компьютера в сети выполните:

```bash
# Проверка доступности
ping 10.24.160.26

# Проверка порта frontend
curl http://10.24.160.26:5173

# Проверка API
curl http://10.24.160.26:5000/api/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"login":"admin","password":"123456"}'
```

---

**Готово!** Все компьютеры в вашей сети теперь могут работать с системой через http://10.24.160.26:5173
