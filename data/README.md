# Файл данных для массовой загрузки инвентаря

## 📋 Описание

Этот файл `inventory.json` позволяет массово добавлять данные инвентаря в систему. Все данные из этого файла будут автоматически загружены в раздел **Инвентаризация**.

## 🚀 Как использовать

### 1. Отредактируйте файл `inventory.json`

Откройте файл `data/inventory.json` и добавьте ваши товары. Каждый товар должен иметь следующую структуру:

```json
{
  "productId": "PROD001",           // ID товара (опционально)
  "name": "Ноутбук Dell XPS 15",    // Название товара (опционально)
  "sku": "A101-DELL-XPS15",         // SKU (ОБЯЗАТЕЛЬНО) - локация автоматически извлекается из SKU
  "quantity": 3,                     // Количество (по умолчанию: 1)
  "condition": "New",                // Состояние: New, Used, Exdisplay, Parts, Faulty
  "length": 36,                      // Длина в см (опционально)
  "width": 24,                       // Ширина в см (опционально)
  "height": 2,                       // Высота в см (опционально)
  "weight": 2,                       // Вес в кг (опционально)
  "price": 85000,                    // Цена товара (опционально)
  "itemId": "397123149682",          // eBay Item ID (опционально)
  "ebayUrl": "https://www.ebay.com/itm/397123149682", // eBay URL (опционально)
  "ebaySellerName": "toponesale",    // Имя продавца eBay (опционально)
  "imageUrl1": "https://i.ebayimg.com/images/g/abc123/s-l1600.jpg", // URL картинки 1
  "imageUrl2": "https://i.ebayimg.com/images/g/abc124/s-l1600.jpg", // URL картинки 2
  // ... до imageUrl24
  "barcodeMappings": [               // Штрихкоды товаров (опционально)
    { "code": "BARCODE001", "qty": 1 },
    { "code": "BARCODE002", "qty": 1 },
    { "code": "BARCODE003", "qty": 1 }
  ]
}
```

### 2. Загрузите данные в базу

Выполните команду:

```bash
tsx server/load-inventory-data.ts
```

### 3. Проверьте результат

- Зайдите в раздел **Инвентаризация** в системе
- Все товары из файла должны отобразиться в таблице
- Проверьте, что данные корректно загрузились

## ⚠️ Важные замечания

1. **SKU обязателен** - каждый товар должен иметь SKU
2. **Локация автоматически извлекается** - из SKU (например, из "A101-DELL-XPS15" извлекается "A101")
3. **Объем вычисляется автоматически** - если указаны длина, ширина и высота
4. **Штрихкоды в формате JSON** - автоматически конвертируются в нужный формат
5. **24 поля для картинок** - можно указать до 24 URL картинок (imageUrl1 - imageUrl24)

## 📸 Поля для картинок

Система поддерживает 24 отдельных поля для URL картинок:
- `imageUrl1`, `imageUrl2`, `imageUrl3`, ..., `imageUrl24`

Картинки будут автоматически отображаться в виде галереи в разделе инвентаризации.

## 🔄 Повторная загрузка

Вы можете редактировать файл `inventory.json` и повторно загружать данные. При этом:
- Если товар с таким же SKU уже существует, будет создан новый товар с новым ID
- Для обновления существующих товаров используйте CSV импорт в интерфейсе системы

## 📝 Примеры

В файле `inventory.json` уже есть 5 примеров товаров. Вы можете:
1. Изучить их структуру
2. Удалить примеры и добавить свои данные
3. Дополнить список новыми товарами

## 🆘 Помощь

Если при загрузке возникли ошибки:
1. Проверьте синтаксис JSON (запятые, кавычки)
2. Убедитесь, что SKU указан для каждого товара
3. Проверьте логи в консоли - там будет информация о каждом товаре
