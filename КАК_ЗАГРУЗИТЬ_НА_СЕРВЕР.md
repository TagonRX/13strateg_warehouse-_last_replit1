# Как загрузить проект на сервер Kubuntu

## Шаг 1: Скачать проект с Replit

### Вариант A: Скачать как ZIP (ПРОЩЕ)

1. В Replit откройте ваш проект
2. Нажмите на кнопку меню (три точки) → **Download as zip**
3. Сохраните файл на ваш компьютер
4. Распакуйте ZIP архив

### Вариант B: Через Git (если настроен)

```bash
# На вашем компьютере
git clone https://github.com/ваш-username/warehouse.git
cd warehouse
```

---

## Шаг 2: Загрузить на сервер

### Вариант A: Через графический SFTP клиент (ПРОЩЕ)

**Программы:** FileZilla, WinSCP, Cyberduck

1. Подключитесь к серверу через SFTP:
   - **Host:** IP адрес вашего сервера
   - **Port:** 22
   - **Username:** ваш логин на сервере
   - **Password:** ваш пароль

2. На сервере создайте папку `/var/www/warehouse`
3. Перетащите все файлы из распакованной папки в `/var/www/warehouse`

### Вариант B: Через командную строку (SCP)

**На Windows (в PowerShell):**

```powershell
# Перейти в папку с проектом
cd C:\путь\к\проекту

# Загрузить на сервер
scp -r * username@server-ip:/var/www/warehouse/
```

**На Linux/Mac:**

```bash
# Перейти в папку с проектом
cd /path/to/project

# Загрузить на сервер
rsync -avz --exclude 'node_modules' --exclude '.git' \
  ./ username@server-ip:/var/www/warehouse/
```

### Вариант C: Через WinSCP (для Windows)

1. Скачать WinSCP: https://winscp.net/
2. Установить и запустить
3. Подключиться к серверу:
   - **File protocol:** SFTP
   - **Host name:** IP адрес сервера
   - **Port:** 22
   - **User name:** ваш логин
   - **Password:** ваш пароль
4. Нажать **Login**
5. Создать папку `/var/www/warehouse` (если нет)
6. Перетащить файлы проекта в `/var/www/warehouse`

---

## Шаг 3: Подключиться к серверу по SSH

### Windows

**Вариант 1: PowerShell (встроенный)**

```powershell
ssh username@server-ip
```

**Вариант 2: PuTTY**

1. Скачать PuTTY: https://www.putty.org/
2. Запустить
3. Host Name: `server-ip`
4. Port: `22`
5. Нажать **Open**
6. Ввести логин и пароль

### Linux/Mac

```bash
ssh username@server-ip
```

---

## Шаг 4: Запустить установку на сервере

После подключения по SSH выполните:

```bash
cd /var/www/warehouse

# Дать права на выполнение скриптов
chmod +x deployment/*.sh

# Запустить автоматическую установку
./deployment/setup.sh
```

Скрипт автоматически:
- Установит Node.js
- Установит PostgreSQL
- Создаст базу данных
- Соберет приложение
- Запустит через PM2
- Настроит Nginx
- Настроит автозапуск

**⏳ Время установки: ~20-30 минут**

---

## Шаг 5: Проверить работу

### Открыть в браузере

```
http://ваш-ip-адрес
```

### Данные для входа

- **Логин:** `admin`
- **Пароль:** `admin123`

⚠️ **Сразу измените пароль!**

---

## Полезная информация

### Узнать IP адрес сервера

**На сервере выполните:**

```bash
ip addr show | grep "inet " | grep -v 127.0.0.1
```

Или:

```bash
hostname -I
```

### Проверить статус приложения

```bash
pm2 status
pm2 logs warehouse
```

### Перезапустить приложение

```bash
pm2 restart warehouse
```

### Остановить приложение

```bash
pm2 stop warehouse
```

---

## Решение проблем

### Не могу подключиться по SSH

1. Проверьте, что SSH сервер запущен на сервере
2. Проверьте IP адрес
3. Проверьте логин и пароль
4. Проверьте, что порт 22 открыт в файрволе

### Скрипт setup.sh выдает ошибку

```bash
# Посмотреть последние строки вывода
tail -n 50

# Попробовать запустить вручную по шагам
# Откройте deployment/DEPLOYMENT_GUIDE.md
```

### Приложение не запускается

```bash
# Посмотреть логи
pm2 logs warehouse --lines 100

# Проверить .env файл
cat .env
```

---

## Дополнительные ресурсы

📖 **Полная инструкция:** `deployment/DEPLOYMENT_GUIDE.md`  
⚡ **Быстрый старт:** `deployment/QUICK_START_RU.md`  
🔧 **Все файлы:** `deployment/README.md`

---

## Структура файлов после загрузки

```
/var/www/warehouse/           ← Корневая папка
├── client/                   ← Frontend код
├── server/                   ← Backend код
├── deployment/               ← Скрипты установки
│   ├── DEPLOYMENT_GUIDE.md   ← Полная инструкция
│   ├── QUICK_START_RU.md     ← Быстрый старт
│   ├── setup.sh              ← Автоустановка
│   ├── backup.sh             ← Резервное копирование
│   ├── restore.sh            ← Восстановление
│   ├── ecosystem.config.js   ← Конфигурация PM2
│   ├── nginx.conf            ← Конфигурация Nginx
│   └── .env.production.example
├── package.json
├── package-lock.json
└── ... другие файлы проекта
```

---

## Контрольный список

- [ ] Проект скачан с Replit
- [ ] Файлы загружены на сервер в `/var/www/warehouse`
- [ ] Подключились к серверу по SSH
- [ ] Запустили `./deployment/setup.sh`
- [ ] Приложение доступно по `http://ip-адрес`
- [ ] Вошли как admin/admin123
- [ ] Изменили пароль администратора
- [ ] (Опционально) Установили SSL сертификат
- [ ] (Опционально) Настроили резервное копирование

---

**Готово!** 🎉

Ваш Warehouse Management System установлен и готов к работе!
