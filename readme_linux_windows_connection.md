# Подключение Windows к Linux серверу (Kubuntu)

## 🎯 Цель
Настроить подключение с Windows 11 компьютера к Linux серверу (Kubuntu) для удаленного управления проектом Warehouse через VS Code.

---

## 📋 Что нужно

### На Linux (Kubuntu) сервере:
- ✅ OpenSSH Server
- ✅ Известный IP адрес
- ✅ Пользователь: `rusan`

### На Windows компьютере:
- ✅ VS Code
- ✅ Расширение Remote-SSH
- ✅ SSH клиент (встроен в Windows 11)

---

## 🔧 Часть 1: Настройка Linux сервера (Kubuntu)

### Шаг 1: Установка OpenSSH Server

⚠️ **Важно:** Для установки нужен пароль sudo. Попробуйте пароль: `1234q`

```bash
# Обновить списки пакетов
sudo apt-get update

# Установить OpenSSH Server
sudo apt-get install openssh-server

# Если появится запрос пароля - введите: 1234q
```

**Альтернатива (если не знаете sudo пароль):**
- Откройте графическое приложение "Discover" или "Software Center"
- Найдите "OpenSSH Server"
- Установите через GUI (может не требовать пароль)

---

### Шаг 2: Запуск SSH сервера

```bash
# Запустить SSH сервер
sudo systemctl start ssh

# Включить автозапуск SSH при загрузке системы
sudo systemctl enable ssh

# Проверить статус
sudo systemctl status ssh
```

**Должно показать:** `Active: active (running)`

---

### Шаг 3: Узнать IP адрес сервера

```bash
# Узнать IP адрес
ip addr show

# Или короче
hostname -I
```

**Найдите IP адрес типа:** `192.168.0.XXX` или `192.168.1.XXX`

📝 **Запишите этот IP адрес** - он понадобится для подключения с Windows!

Пример: `192.168.0.118`

---

### Шаг 4: Проверка SSH (опционально)

```bash
# Проверить что SSH слушает порт 22
sudo ss -tulpn | grep :22

# Или
sudo netstat -tulpn | grep :22
```

---

### Шаг 5: Настройка Firewall (если включен)

```bash
# Проверить статус firewall
sudo ufw status

# Если firewall активен - разрешить SSH
sudo ufw allow ssh
sudo ufw allow 22/tcp

# Разрешить порты приложения
sudo ufw allow 5173/tcp
sudo ufw allow 5000/tcp
```

---

## 💻 Часть 2: Настройка Windows компьютера

### Шаг 1: Установка VS Code

1. Скачайте VS Code: https://code.visualstudio.com/
2. Установите (Next → Next → Install)
3. Запустите VS Code

---

### Шаг 2: Установка расширения Remote-SSH

1. В VS Code нажмите `Ctrl+Shift+X` (или кликните иконку Extensions слева)
2. В поиске введите: **Remote - SSH**
3. Найдите расширение от **Microsoft**
4. Нажмите **Install**

---

### Шаг 3: Проверка SSH клиента в Windows

Откройте PowerShell или Windows Terminal:

```powershell
# Проверить что SSH установлен
ssh -V

# Должно показать версию, например: OpenSSH_for_Windows_8.x
```

Если команда не найдена - SSH нужно включить в Windows:
1. `Settings` → `Apps` → `Optional Features`
2. Найдите `OpenSSH Client`
3. Установите если нет

---

### Шаг 4: Первое подключение по SSH (тест)

В PowerShell:

```powershell
# Заменить 192.168.0.118 на ваш IP адрес Linux сервера
ssh rusan@192.168.0.118
```

**Что произойдет:**
1. Появится вопрос: `Are you sure you want to continue connecting?` → Напечатайте `yes` и Enter
2. Попросит пароль пользователя `rusan` → Введите пароль
3. Если всё ок - вы окажетесь в терминале Linux сервера
4. Введите `exit` чтобы выйти

---

### Шаг 5: Подключение через VS Code Remote-SSH

1. В VS Code нажмите `F1` (или `Ctrl+Shift+P`)
2. Введите: `Remote-SSH: Connect to Host...`
3. Выберите `+ Add New SSH Host...`
4. Введите: `ssh rusan@192.168.0.118` (замените на ваш IP)
5. Нажмите Enter
6. Выберите файл конфигурации (обычно первый: `C:\Users\ВашИмя\.ssh\config`)
7. Нажмите `Connect`
8. Выберите платформу: `Linux`
9. Введите пароль пользователя `rusan`

**VS Code перезагрузится и подключится к серверу!**

---

### Шаг 6: Открытие папки проекта

После подключения:

1. `File` → `Open Folder...` (или `Ctrl+K Ctrl+O`)
2. В поле введите: `/home/rusan/Desktop/13strateg_warehouse-_last_replit1`
3. Нажмите `OK`
4. Введите пароль (если попросит)

✅ **Готово!** Теперь вы видите файлы проекта и можете их редактировать!

---

### Шаг 7: Использование терминала

1. В VS Code нажмите ``Ctrl+` `` (или `Terminal` → `New Terminal`)
2. Откроется терминал **Linux сервера**
3. Можете выполнять команды:

```bash
# Проверить статус PM2
pm2 list

# Перезапустить сервер
pm2 restart warehouse

# Просмотреть логи
pm2 logs warehouse --lines 50

# Обновить проект
git pull origin main
npm install
```

---

## 🔑 Настройка входа без пароля (SSH ключи)

Чтобы не вводить пароль каждый раз при подключении:

### На Windows (PowerShell):

```powershell
# 1. Создать SSH ключ (если еще нет)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Нажимайте Enter на все вопросы (без пароля для ключа)
# Enter file in which to save the key: [Enter]
# Enter passphrase: [Enter]
# Enter same passphrase again: [Enter]

# 2. Скопировать публичный ключ на сервер
type $env:USERPROFILE\.ssh\id_ed25519.pub | ssh rusan@192.168.0.118 "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"

# Введите пароль Linux сервера в последний раз
```

### На Linux сервере (для безопасности):

```bash
# Установить правильные права доступа
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### Проверка:

```powershell
# На Windows попробуйте подключиться
ssh rusan@192.168.0.118

# Должно подключиться БЕЗ запроса пароля!
```

✅ **Теперь VS Code подключается автоматически без пароля!**

---

## 🌐 Доступ к веб-интерфейсу с Windows

После того как сервер запущен на Linux:

```bash
# На Linux сервере проверить что сервер работает
pm2 list
```

**На Windows откройте браузер:**

- Frontend: `http://192.168.0.118:5173`
- Backend API: `http://192.168.0.118:5000`

(Замените IP на адрес вашего сервера)

---

## 🔄 Типичный рабочий процесс

### На Windows компьютере:

1. **Открыть VS Code**
2. `F1` → `Remote-SSH: Connect to Host...` → Выбрать ваш сервер
3. Открыть папку проекта (если не открыта)
4. Редактировать файлы
5. В терминале VS Code выполнять команды:
   ```bash
   git pull origin main
   npm install
   pm2 restart warehouse
   pm2 logs warehouse
   ```
6. Открыть браузер: `http://192.168.0.118:5173`

---

## 🔒 Настройка статического IP (опционально)

Чтобы IP адрес сервера не менялся после перезагрузки:

### Вариант 1: Резервация на роутере (рекомендуется)

**На Linux узнать MAC адрес:**
```bash
ip link show
# Найти MAC адрес (например: 00:11:22:33:44:55)
```

**На роутере:**
1. Зайти в веб-интерфейс роутера: `http://192.168.0.1` или `http://192.168.1.1`
2. Найти раздел "DHCP" → "DHCP Reservation" или "Резервация адресов"
3. Добавить:
   - MAC адрес: `00:11:22:33:44:55`
   - IP адрес: `192.168.0.100` (например)
4. Сохранить
5. Перезагрузить Linux сервер: `sudo reboot`

Теперь сервер всегда будет иметь IP `192.168.0.100`

---

### Вариант 2: Статический IP в самом Linux

```bash
# Узнать имя сетевого интерфейса
ip link show
# Например: enp3s0, ens33, eth0

# Создать конфигурацию netplan
sudo nano /etc/netplan/01-netcfg.yaml
```

Вставить (замените значения на свои):

```yaml
network:
  version: 2
  renderer: networkd
  ethernets:
    enp3s0:  # ← Замените на ваш интерфейс
      dhcp4: no
      addresses:
        - 192.168.0.100/24  # ← Желаемый IP
      routes:
        - to: default
          via: 192.168.0.1  # ← IP роутера
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

```bash
# Применить
sudo netplan try  # Проверка (откатится через 120 сек если ошибка)
sudo netplan apply  # Применить

# Проверить
ip addr show
```

---

## 📱 Управление с телефона (бонус)

### Android:
- **Termux** - полноценный терминал
- **JuiceSSH** - SSH клиент

### iOS:
- **Terminus** - SSH + SFTP
- **Blink Shell** - терминал

Подключение такое же: `ssh rusan@192.168.0.118`

---

## 🐛 Решение проблем

### SSH сервер не запускается

```bash
# Проверить статус
sudo systemctl status ssh

# Посмотреть ошибки
sudo journalctl -u ssh -n 50

# Переустановить
sudo apt-get remove --purge openssh-server
sudo apt-get install openssh-server
```

---

### Не могу подключиться с Windows

**Проверка 1: Ping**
```powershell
ping 192.168.0.118
```
Если не пингуется - проблема с сетью.

**Проверка 2: SSH порт**
```powershell
Test-NetConnection -ComputerName 192.168.0.118 -Port 22
```

**Проверка 3: Firewall на Linux**
```bash
# Проверить
sudo ufw status

# Разрешить SSH
sudo ufw allow 22/tcp
```

---

### VS Code не может подключиться

1. Проверить SSH в командной строке Windows:
   ```powershell
   ssh rusan@192.168.0.118 -v
   ```
   `-v` покажет подробные логи

2. Удалить кэш VS Code Remote:
   - Закрыть VS Code
   - Удалить папку: `C:\Users\ВашИмя\.vscode\extensions`
   - Переустановить расширение Remote-SSH

3. Проверить файл конфигурации SSH:
   ```powershell
   notepad $env:USERPROFILE\.ssh\config
   ```
   
   Должно быть:
   ```
   Host 192.168.0.118
     HostName 192.168.0.118
     User rusan
   ```

---

### Забыли пароль sudo

**Варианты:**
1. Попробовать: `1234q`, `1234`, `admin`, `rusan`, пустой пароль (Enter)
2. Проверить есть ли наклейка/документация от установки системы
3. Сброс пароля через Recovery Mode (требует физический доступ к компьютеру)

**Для сброса пароля через Recovery:**
1. Перезагрузить компьютер
2. При загрузке зажать `Shift` (или `Esc`) для входа в GRUB
3. Выбрать "Advanced options" → "Recovery mode"
4. Выбрать "root" (root shell)
5. Выполнить:
   ```bash
   passwd rusan  # Установить новый пароль
   reboot
   ```

---

## 📋 Чек-лист настройки

### Linux сервер (Kubuntu):
- [ ] OpenSSH Server установлен
- [ ] SSH сервис запущен: `sudo systemctl status ssh`
- [ ] Узнан IP адрес: `hostname -I`
- [ ] Firewall настроен (если включен): `sudo ufw allow 22/tcp`
- [ ] PM2 сервер запущен: `pm2 list`
- [ ] (Опционально) Настроен статический IP

### Windows компьютер:
- [ ] VS Code установлен
- [ ] Расширение Remote-SSH установлено
- [ ] SSH клиент работает: `ssh -V`
- [ ] Успешное подключение: `ssh rusan@IP_АДРЕС`
- [ ] VS Code подключается к серверу
- [ ] Открыта папка проекта
- [ ] (Опционально) Настроены SSH ключи для входа без пароля

### Проверка работы:
- [ ] Веб-интерфейс открывается: `http://IP:5173`
- [ ] Можно редактировать файлы в VS Code
- [ ] Терминал VS Code работает
- [ ] Git команды выполняются
- [ ] PM2 управление работает

---

## 🎯 Быстрая шпаргалка

### На Linux (Kubuntu):
```bash
# Установить SSH
sudo apt-get install openssh-server
sudo systemctl enable ssh --now

# Узнать IP
hostname -I

# Статус сервера
pm2 list
pm2 logs warehouse
```

### На Windows:
```powershell
# Подключиться по SSH
ssh rusan@192.168.0.118

# Или через VS Code:
# F1 → Remote-SSH: Connect to Host
```

### Адреса:
- SSH: `ssh rusan@192.168.0.118`
- Web: `http://192.168.0.118:5173`
- API: `http://192.168.0.118:5000`

---

## 🔗 Полезные ссылки

- VS Code: https://code.visualstudio.com/
- VS Code Remote SSH документация: https://code.visualstudio.com/docs/remote/ssh
- OpenSSH документация: https://www.openssh.com/
- Netplan документация: https://netplan.io/

---

## 📞 Следующие шаги

После успешного подключения вы можете:
1. Работать с кодом через VS Code на Windows
2. Управлять сервером через терминал VS Code
3. Обновлять проект с GitHub
4. Просматривать логи и мониторить приложение
5. Открывать веб-интерфейс в браузере Windows

**Удачи!** 🚀
