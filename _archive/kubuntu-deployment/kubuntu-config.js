// ═══════════════════════════════════════════════════════════════════
// КОНФИГУРАЦИЯ WAREHOUSE MANAGEMENT SYSTEM
// ═══════════════════════════════════════════════════════════════════
//
// ✅ ЗДЕСЬ ВСЕ НАСТРОЙКИ - ПРОСТО ИЗМЕНИТЕ НУЖНЫЕ ЗНАЧЕНИЯ
//
// Этот файл используется всеми скриптами:
//   • drizzle.config.ts (миграции базы данных)
//   • import-data.js (загрузка данных)
//   • install.sh (установка)
//   • start.sh (запуск сервера)
//
// ═══════════════════════════════════════════════════════════════════

module.exports = {
  
  // ─────────────────────────────────────────────────────────────────
  // 1️⃣  НАСТРОЙКИ БАЗЫ ДАННЫХ PostgreSQL
  // ─────────────────────────────────────────────────────────────────
  database: {
    // Адрес сервера PostgreSQL (обычно localhost для локальной установки)
    host: 'localhost',
    
    // Порт PostgreSQL (стандартный 5432)
    port: 5432,
    
    // Имя пользователя PostgreSQL
    user: 'postgres',
    
    // Пароль для PostgreSQL
    // ⚠️ ВАЖНО: Если используете режим trust (БЕЗ пароля), оставьте пустым ''
    // Если PostgreSQL требует пароль - впишите его сюда
    password: '',
    
    // Название базы данных
    name: 'warehouse_local'
  },

  // ─────────────────────────────────────────────────────────────────
  // 2️⃣  ПУТИ К ФАЙЛАМ
  // ─────────────────────────────────────────────────────────────────
  paths: {
    // Откуда загружать данные (72,439 записей)
    // Можно указать абсолютный путь: '/home/ruslan/Desktop/warehouse/database-export.json'
    // Или относительный от корня проекта: './database-export.json'
    dataFile: './database-export.json',
    
    // Корневая папка проекта
    // Обычно не нужно менять - автоматически определяется
    projectRoot: process.cwd()
  },

  // ─────────────────────────────────────────────────────────────────
  // 3️⃣  НАСТРОЙКИ ВЕБ-СЕРВЕРА
  // ─────────────────────────────────────────────────────────────────
  server: {
    // На каком порту запускать веб-сервер
    // После запуска открывайте: http://localhost:5000
    port: 5000,
    
    // На каком интерфейсе слушать
    // '0.0.0.0' = доступен по сети
    // 'localhost' = только с этого компьютера
    host: '0.0.0.0'
  },

  // ─────────────────────────────────────────────────────────────────
  // 4️⃣  ЛОГИ И ОТЛАДКА
  // ─────────────────────────────────────────────────────────────────
  logging: {
    // Файл для логов сервера
    logFile: './warehouse.log',
    
    // Уровень детализации логов: 'error', 'warn', 'info', 'debug'
    logLevel: 'info'
  },

  // ─────────────────────────────────────────────────────────────────
  // 5️⃣  ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
  // ─────────────────────────────────────────────────────────────────
  advanced: {
    // Тайм-аут подключения к базе данных (миллисекунды)
    dbTimeout: 30000,
    
    // Максимум подключений к PostgreSQL
    maxDbConnections: 20,
    
    // Автоматически создавать таблицы при старте
    autoMigrate: true,
    
    // Автоматически импортировать данные при установке
    autoImportData: true
  }
};

// ═══════════════════════════════════════════════════════════════════
// ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ:
// ═══════════════════════════════════════════════════════════════════
//
// Если PostgreSQL требует пароль:
//   password: 'мой_пароль_123'
//
// Если данные в другом месте:
//   dataFile: '/home/ruslan/Downloads/my-data.json'
//
// Если хотите другой порт:
//   port: 8080
//   (тогда открывайте http://localhost:8080)
//
// ═══════════════════════════════════════════════════════════════════
