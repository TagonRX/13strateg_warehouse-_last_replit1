# Быстрый старт - Развертывание на Kubuntu

**⏱️ Время установки: ~30-40 минут**

## Минимальные требования

- Kubuntu/Ubuntu 22.04 или новее
- 2GB RAM (рекомендуется 4GB)
- 20GB свободного места
- Доступ к интернету
- sudo права

---

## Вариант 1: Автоматическая установка (РЕКОМЕНДУЕТСЯ) ⚡

### Шаг 1: Загрузить проект на сервер

**На вашем компьютере (где находится проект):**

```bash
# Скачать проект как ZIP из Replit или Git
# Затем загрузить на сервер через SCP/SFTP

# ИЛИ использовать rsync:
rsync -avz --exclude 'node_modules' --exclude '.git' \
  /path/to/project/ user@server-ip:/var/www/warehouse/
```

### Шаг 2: Запустить автоматическую установку

**На сервере:**

```bash
cd /var/www/warehouse
chmod +x deployment/setup.sh
./deployment/setup.sh
```

Скрипт спросит:
- Имя базы данных (Enter для `warehouse_db`)
- Пользователя БД (Enter для `warehouse_user`)
- Пароль БД (Enter для автогенерации)
- Домен/IP адрес вашего сервера

**⏳ Подождите ~20-30 минут**

### Шаг 3: Готово! 🎉

Откройте в браузере: `http://ваш-ip-адрес`

**Логин:** `admin`  
**Пароль:** `admin123` (измените при первом входе!)

---

## Вариант 2: Ручная установка по шагам

Если автоматическая установка не сработала, следуйте детальной инструкции:

👉 **Откройте `deployment/DEPLOYMENT_GUIDE.md`**

Там есть все команды с объяснениями.

---

## Что делать после установки?

### 1. Изменить пароль администратора ⚠️

1. Войти в систему (`admin` / `admin123`)
2. Admin Panel → Users
3. Изменить пароль на надежный

### 2. Установить SSL сертификат (если есть домен) 🔒

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d ваш-домен.com
```

### 3. Настроить автоматическое резервное копирование 💾

```bash
crontab -e
# Добавить строку:
0 2 * * * /var/www/warehouse/deployment/backup.sh
```

---

## Полезные команды

### Проверка статуса

```bash
pm2 status              # Статус приложения
pm2 logs warehouse      # Логи приложения
sudo systemctl status nginx         # Статус Nginx
sudo systemctl status postgresql    # Статус PostgreSQL
```

### Управление приложением

```bash
pm2 restart warehouse   # Перезапуск
pm2 stop warehouse      # Остановка
pm2 start warehouse     # Запуск
```

### Резервное копирование

```bash
./deployment/backup.sh  # Создать резервную копию
ls -lh backups/         # Список резервных копий
```

---

## Что-то пошло не так? 🆘

### Приложение не запускается

```bash
pm2 logs warehouse --lines 100
```

Ищите строки с `ERROR` или `FATAL`.

### Не могу зайти в админку

1. Проверить, что приложение запущено: `pm2 status`
2. Проверить логи: `pm2 logs warehouse`
3. Проверить Nginx: `sudo systemctl status nginx`

### WebSocket не работает (barcode scanner)

```bash
sudo nano /etc/nginx/sites-available/warehouse
# Убедитесь, что есть секция location /ws
sudo systemctl restart nginx
```

### База данных не подключается

```bash
# Проверить статус
sudo systemctl status postgresql

# Проверить настройки
cat .env | grep DATABASE_URL

# Тест подключения
psql -U warehouse_user -d warehouse_db -h localhost -W
```

---

## Обновление приложения

```bash
cd /var/www/warehouse
pm2 stop warehouse
git pull origin main  # или загрузить новые файлы
npm install --production
npm run build
npm run db:push
pm2 start warehouse
```

---

## Дополнительная помощь

📖 **Полная документация:** `deployment/DEPLOYMENT_GUIDE.md`  
🔧 **Конфигурация PM2:** `deployment/ecosystem.config.cjs`  
🌐 **Конфигурация Nginx:** `deployment/nginx.conf`  
💾 **Резервное копирование:** `deployment/backup.sh`  

---

## Файлы проекта на сервере

```
/var/www/warehouse/
├── .env                    # ⚙️ Настройки (пароли, секреты)
├── dist/                   # 📦 Скомпилированное приложение
├── deployment/             # 🚀 Скрипты развертывания
├── logs/                   # 📝 Логи PM2
└── backups/                # 💾 Резервные копии БД
```

---

**Успешного развертывания!** 🚀

Если возникли вопросы, изучите полную документацию в `DEPLOYMENT_GUIDE.md`.
