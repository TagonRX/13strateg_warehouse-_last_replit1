# Production Environment Variables
# Copy this file to .env in the root directory and fill in the values

# Node Environment
NODE_ENV=production
PORT=5000
HOST=0.0.0.0

# PostgreSQL Database
# Format: postgresql://USERNAME:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://warehouse_user:your_strong_password@localhost:5432/warehouse_db

# Session Secret (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your_generated_session_secret_here_minimum_32_characters

# Server Domain (optional, for production deployments)
# If using a domain, set it here. Otherwise, use your server IP.
REPLIT_DOMAINS=your-domain.com

# Optional: PostgreSQL individual connection params (alternative to DATABASE_URL)
# PGHOST=localhost
# PGPORT=5432
# PGUSER=warehouse_user
# PGPASSWORD=your_strong_password
# PGDATABASE=warehouse_db

# Optional: Additional settings
# MAX_UPLOAD_SIZE=50mb
# LOG_LEVEL=info
