# 🔄 Настройка синхронизации с GitHub

Эта инструкция поможет вам настроить автоматическую синхронизацию проекта между вашим компьютером, GitHub и сервером Ubuntu.

---

## 📋 Схема работы

```
Ваш компьютер → GitHub → Ubuntu Server
     ↓              ↓           ↓
  git push      хранилище   git pull
                             + backup
```

**Преимущества:**
- ✅ Версионный контроль всех изменений
- ✅ Автоматическое резервное копирование перед обновлением
- ✅ Откат к любой предыдущей версии одной командой
- ✅ Безопасное обновление на сервере

---

## 🚀 Часть 1: Настройка GitHub (на вашем компьютере)

### Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com
2. Нажмите кнопку "New repository" (зеленая кнопка)
3. Заполните:
   - **Repository name**: `warehouse-management` (или свое название)
   - **Description**: Warehouse Management System
   - **Visibility**: Private (рекомендуется) или Public
   - ❌ **НЕ создавайте** README, .gitignore, license (они уже есть)
4. Нажмите "Create repository"

### Шаг 2: Подключите локальный проект к GitHub

На вашем компьютере, в папке проекта:

```bash
# Инициализируем Git (если еще не сделано)
git init

# Добавляем все файлы
git add .

# Создаем первый коммит
git commit -m "Initial commit - Warehouse Management System"

# Подключаем GitHub репозиторий
# ⚠️ ЗАМЕНИТЕ на ваш URL!
git remote add origin https://github.com/ВАШ_ЛОГИН/warehouse-management.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

**⚠️ Важно:** Замените `ВАШ_ЛОГИН/warehouse-management` на ваш реальный URL!

### Шаг 3: Настройте аутентификацию

GitHub требует аутентификации. Два способа:

#### Способ A: Personal Access Token (рекомендуется)

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Настройки:
   - **Note**: `Warehouse Server Access`
   - **Expiration**: `No expiration` (или на ваш выбор)
   - **Scopes**: отметьте `repo` (полный доступ к репозиториям)
4. Нажмите "Generate token"
5. **СКОПИРУЙТЕ ТОКЕН!** (он больше не отобразится)

Теперь при git push используйте:
```bash
Username: ваш_github_логин
Password: скопированный_токен (не реальный пароль!)
```

#### Способ B: SSH ключи (для продвинутых)

```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "your_email@example.com"

# Добавление ключа в SSH agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Скопировать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавить в GitHub:
# https://github.com/settings/keys
```

Используйте SSH URL:
```bash
git remote set-url origin git@github.com:ВАШ_ЛОГИН/warehouse-management.git
```

---

## 🖥️ Часть 2: Настройка на Ubuntu Server

### Шаг 1: Установите Git на сервере

```bash
sudo apt-get update
sudo apt-get install -y git
```

### Шаг 2: Настройте доступ к GitHub

#### Для HTTPS (с токеном):

```bash
# Настройка Git credentials
git config --global credential.helper store

# При первом git clone введите:
# Username: ваш_github_логин
# Password: ваш_personal_access_token
```

#### Для SSH:

```bash
# Генерация SSH ключа на сервере
ssh-keygen -t ed25519 -C "server@warehouse"

# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавьте этот ключ на GitHub:
# https://github.com/settings/keys
```

### Шаг 3: Первая синхронизация

После установки приложения (скрипты 1-5), настройте синхронизацию:

```bash
cd /opt/warehouse

# Инициализация Git репозитория
git init

# Подключение к GitHub
# HTTPS:
git remote add origin https://github.com/ВАШ_ЛОГИН/warehouse-management.git

# ИЛИ SSH:
git remote add origin git@github.com:ВАШ_ЛОГИН/warehouse-management.git

# Получение кода
git fetch origin main
git reset --hard origin/main
```

### Шаг 4: Настройте переменную окружения для скрипта

```bash
# Добавьте в ~/.bashrc или /etc/environment
export GITHUB_REPO_URL="https://github.com/ВАШ_ЛОГИН/warehouse-management.git"
export GITHUB_BRANCH="main"

# Применить изменения
source ~/.bashrc
```

---

## 🔄 Часть 3: Ежедневная работа

### На вашем компьютере (разработка):

```bash
# 1. Внесите изменения в код
# 2. Сохраните изменения в Git:

git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере Ubuntu (обновление):

```bash
# Синхронизация с GitHub (автоматически создаст резервную копию)
sudo bash /opt/warehouse/deployment/6-sync-github.sh
```

**Что происходит:**
1. ✅ Создается резервная копия текущей версии
2. ✅ Скачиваются изменения с GitHub
3. ✅ Обновляются зависимости (npm install)
4. ✅ Применяются изменения БД (если есть)
5. ✅ Перезапускается сервис
6. ✅ Старые резервные копии очищаются (сохраняется 10 последних)

---

## 🔙 Часть 4: Откат к предыдущей версии

### Просмотр доступных резервных копий:

```bash
sudo bash /opt/warehouse/deployment/7-rollback.sh list
```

### Откат к последней резервной копии:

```bash
sudo bash /opt/warehouse/deployment/7-rollback.sh
```

### Откат к конкретной резервной копии:

```bash
sudo bash /opt/warehouse/deployment/7-rollback.sh warehouse_backup_2025-01-15_14-30-22.tar.gz
```

---

## 📁 Где хранятся резервные копии

```
/opt/warehouse-backups/
├── warehouse_backup_2025-01-15_14-30-22.tar.gz    (5.2 MB)
├── warehouse_backup_2025-01-15_16-45-10.tar.gz    (5.3 MB)
├── warehouse_backup_2025-01-16_09-15-33.tar.gz    (5.4 MB)
├── .last_backup                                    (путь к последней)
├── .backup_2025-01-15_14-30-22_info.txt          (информация о версии)
└── ...
```

**Автоматическая очистка:** Сохраняются только последние 10 копий.

---

## 🛡️ Безопасность

### Что НЕ попадает в Git (защищено .gitignore):

- ❌ `node_modules/` (устанавливаются автоматически)
- ❌ `*.env`, `warehouse.env` (пароли БД)
- ❌ `data/arhiv/*.csv` (архивы изменений)
- ❌ Резервные копии
- ❌ Логи

### Что попадает в Git:

- ✅ Исходный код (client/, server/, shared/)
- ✅ Скрипты развертывания (deployment/)
- ✅ Конфигурация (package.json, tsconfig.json)
- ✅ Документация (README.md, replit.md)
- ✅ Шаблон CSV (data/inventory_sync.csv - пустой)

---

## ⚙️ Автоматизация (опционально)

### Автоматическое обновление каждый день в 3:00 ночи:

```bash
# Добавьте в crontab
sudo crontab -e

# Добавьте строку:
0 3 * * * /opt/warehouse/deployment/6-sync-github.sh >> /var/log/warehouse-sync.log 2>&1
```

### Или создайте systemd timer (более современно):

```bash
# /etc/systemd/system/warehouse-sync.timer
[Unit]
Description=Warehouse GitHub Sync Timer

[Timer]
OnCalendar=daily
OnCalendar=03:00
Persistent=true

[Install]
WantedBy=timers.target
```

```bash
# /etc/systemd/system/warehouse-sync.service
[Unit]
Description=Warehouse GitHub Sync

[Service]
Type=oneshot
ExecStart=/opt/warehouse/deployment/6-sync-github.sh
```

```bash
# Активировать
sudo systemctl enable warehouse-sync.timer
sudo systemctl start warehouse-sync.timer
```

---

## 🆘 Решение проблем

### Ошибка: "Authentication failed"

**Решение:**
```bash
# Проверьте учетные данные
git config --global credential.helper store

# Повторите git pull и введите правильный токен
```

### Ошибка: "Conflict" при git pull

**Решение:**
```bash
# Скрипт автоматически делает stash локальных изменений
# Или вручную:
git stash
git pull origin main
git stash pop
```

### Сервис не запускается после обновления

**Решение:**
```bash
# Откат к предыдущей версии
sudo bash /opt/warehouse/deployment/7-rollback.sh

# Проверка логов
sudo journalctl -u warehouse -n 100
```

---

## 📊 Сводная таблица команд

| Действие | Команда |
|----------|---------|
| **На компьютере:** Отправить изменения | `git add . && git commit -m "..." && git push` |
| **На сервере:** Обновить с GitHub | `sudo bash 6-sync-github.sh` |
| **На сервере:** Список резервных копий | `sudo bash 7-rollback.sh list` |
| **На сервере:** Откат к последней копии | `sudo bash 7-rollback.sh` |
| **На сервере:** Откат к конкретной копии | `sudo bash 7-rollback.sh имя_файла.tar.gz` |
| **Просмотр логов** | `sudo journalctl -u warehouse -f` |
| **Статус сервиса** | `sudo systemctl status warehouse` |

---

## ✅ Готово!

Теперь у вас:
- ✅ Версионный контроль через GitHub
- ✅ Автоматическое резервное копирование
- ✅ Простое обновление на сервере
- ✅ Откат одной командой
- ✅ Сохранение 10 последних версий

**Успешной работы! 🎉**
