# Руководство по развертыванию Warehouse Management System на Kubuntu

Полное пошаговое руководство от чистого сервера до работающего приложения.

---

## Требования к серверу

- **ОС**: Ubuntu 22.04 / 24.04 или Kubuntu
- **RAM**: Минимум 2GB (рекомендуется 4GB+)
- **Диск**: Минимум 20GB свободного места
- **CPU**: 2+ ядра
- **Доступ**: sudo права

---

## ЧАСТЬ 1: Базовая настройка сервера (10 минут)

### Шаг 1.1: Обновление системы

```bash
# Обновить список пакетов
sudo apt update

# Обновить установленные пакеты
sudo apt upgrade -y

# Установить базовые утилиты
sudo apt install -y curl wget git build-essential
```

### Шаг 1.2: Настройка файрвола (UFW)

```bash
# Разрешить SSH (важно, чтобы не потерять доступ!)
sudo ufw allow OpenSSH

# Разрешить HTTP и HTTPS
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Включить файрвол
sudo ufw enable

# Проверить статус
sudo ufw status
```

---

## ЧАСТЬ 2: Установка Node.js (5 минут)

```bash
# Добавить репозиторий NodeSource для Node.js 20 LTS
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

# Установить Node.js и npm
sudo apt-get install -y nodejs

# Проверить установку
node -v    # Должно показать v20.x.x
npm -v     # Должно показать 10.x.x
```

---

## ЧАСТЬ 3: Установка PostgreSQL (10 минут)

### Шаг 3.1: Установка PostgreSQL

```bash
# Установить PostgreSQL
sudo apt install -y postgresql postgresql-contrib

# Запустить и включить автозапуск
sudo systemctl start postgresql
sudo systemctl enable postgresql

# Проверить статус
sudo systemctl status postgresql
```

### Шаг 3.2: Создание базы данных и пользователя

```bash
# Войти в psql от имени postgres
sudo -u postgres psql

# Выполнить следующие команды в psql:
```

```sql
-- Создать пользователя (замените 'ваш_пароль' на свой надежный пароль)
CREATE ROLE warehouse_user WITH LOGIN PASSWORD 'ваш_пароль';

-- Дать права на создание БД
ALTER ROLE warehouse_user CREATEDB;

-- Создать базу данных
CREATE DATABASE warehouse_db OWNER warehouse_user;

-- Выйти из psql
\q
```

### Шаг 3.3: Тест подключения

```bash
# Проверить подключение (введите пароль когда попросят)
psql -U warehouse_user -d warehouse_db -h localhost -W

# Если подключение успешно, выйти:
\q
```

---

## ЧАСТЬ 4: Развертывание приложения (15 минут)

### Шаг 4.1: Создание директории приложения

```bash
# Создать директорию для приложения
sudo mkdir -p /var/www/warehouse
sudo chown $USER:$USER /var/www/warehouse
cd /var/www/warehouse
```

### Шаг 4.2: Загрузка файлов проекта

**Вариант A: Через Git (рекомендуется)**

```bash
# Если у вас есть Git репозиторий:
git clone https://your-repo-url.git .
```

**Вариант B: Через архив ZIP**

```bash
# На вашем компьютере:
# 1. Скачать проект как .zip из Replit (Download as zip)
# 2. Загрузить на сервер через SCP/SFTP

# На сервере:
# Распаковать архив
unzip warehouse.zip
mv warehouse/* .
rmdir warehouse
```

**Вариант C: Через rsync**

```bash
# На вашем локальном компьютере (где находится проект):
rsync -avz --exclude 'node_modules' --exclude '.git' \
  /path/to/local/project/ \
  user@server-ip:/var/www/warehouse/
```

### Шаг 4.3: Настройка переменных окружения

```bash
# Создать production .env файл
nano /var/www/warehouse/.env
```

Вставьте следующее содержимое (замените значения на свои):

```env
NODE_ENV=production
PORT=5000

# PostgreSQL Connection
DATABASE_URL=postgresql://warehouse_user:ваш_пароль@localhost:5432/warehouse_db

# Session Secret (сгенерируйте случайную строку)
SESSION_SECRET=сгенерируйте_случайную_строку_минимум_32_символа

# Server Settings (используйте ваш домен или IP)
REPLIT_DOMAINS=your-domain.com
HOST=0.0.0.0
```

**Для генерации SESSION_SECRET выполните:**

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат и вставьте в .env как SESSION_SECRET.

### Шаг 4.4: Установка зависимостей

```bash
cd /var/www/warehouse

# Установить production зависимости
npm install --production

# Если нужен dev build, установите все зависимости:
npm install
```

### Шаг 4.5: Сборка приложения

```bash
# Собрать frontend и backend
npm run build

# Проверить, что появилась папка dist/
ls -la dist/
```

### Шаг 4.6: Настройка базы данных

```bash
# Применить схему БД (создать таблицы)
npm run db:push

# Если появится предупреждение о потере данных, используйте force:
# npm run db:push -- --force

# Создать первого администратора
node dist/hash-admin-password.js

# Создать начальные настройки склада
node dist/seed-warehouse-settings.js
```

---

## ЧАСТЬ 5: Установка PM2 (Process Manager) (5 минут)

```bash
# Установить PM2 глобально
sudo npm install -g pm2

# Запустить приложение через PM2
cd /var/www/warehouse
pm2 start ecosystem.config.js --env production

# Настроить автозапуск при перезагрузке
pm2 startup systemd

# Скопируйте и выполните команду, которую покажет PM2
# (она будет выглядеть примерно так: sudo env PATH=...)

# Сохранить список процессов
pm2 save

# Проверить статус
pm2 status
pm2 logs warehouse
```

---

## ЧАСТЬ 6: Установка и настройка Nginx (10 минут)

### Шаг 6.1: Установка Nginx

```bash
sudo apt install -y nginx

# Запустить и включить автозапуск
sudo systemctl start nginx
sudo systemctl enable nginx
```

### Шаг 6.2: Настройка виртуального хоста

```bash
# Создать конфигурацию
sudo nano /etc/nginx/sites-available/warehouse
```

Вставьте следующую конфигурацию (замените your-domain.com на ваш домен или IP):

```nginx
# Upstream для Node.js приложения
upstream warehouse_app {
    server 127.0.0.1:5000;
    keepalive 64;
}

server {
    listen 80;
    listen [::]:80;
    server_name your-domain.com www.your-domain.com;  # Замените на ваш домен или IP

    # Логи
    access_log /var/log/nginx/warehouse_access.log;
    error_log /var/log/nginx/warehouse_error.log;

    # Размер загружаемых файлов
    client_max_body_size 50M;

    # Основное приложение
    location / {
        proxy_pass http://warehouse_app;
        proxy_http_version 1.1;
        
        # WebSocket support (для barcode scanner)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Прокси заголовки
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-NginX-Proxy true;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # WebSocket endpoint (важно для remote scanner)
    location /ws {
        proxy_pass http://warehouse_app;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 86400;
    }
}
```

### Шаг 6.3: Активация конфигурации

```bash
# Создать символическую ссылку
sudo ln -s /etc/nginx/sites-available/warehouse /etc/nginx/sites-enabled/

# Удалить дефолтный сайт (опционально)
sudo rm /etc/nginx/sites-enabled/default

# Проверить конфигурацию
sudo nginx -t

# Если тест прошел успешно, перезапустить Nginx
sudo systemctl restart nginx
```

---

## ЧАСТЬ 7: SSL сертификат (Let's Encrypt) - ОПЦИОНАЛЬНО (5 минут)

**Только если у вас есть домен!**

```bash
# Установить Certbot
sudo apt install -y certbot python3-certbot-nginx

# Получить SSL сертификат (замените email и домен)
sudo certbot --nginx -d your-domain.com -d www.your-domain.com \
  --non-interactive --agree-tos -m your-email@example.com

# Автопродление настроено автоматически
# Проверить таймер автопродления:
sudo systemctl status certbot.timer

# Тест продления (без фактического продления):
sudo certbot renew --dry-run
```

После установки SSL, Nginx автоматически перенаправит HTTP на HTTPS.

---

## ЧАСТЬ 8: Проверка работы (5 минут)

### Шаг 8.1: Проверить все сервисы

```bash
# Статус PostgreSQL
sudo systemctl status postgresql

# Статус Nginx
sudo systemctl status nginx

# Статус приложения через PM2
pm2 status

# Логи приложения
pm2 logs warehouse --lines 50
```

### Шаг 8.2: Открыть в браузере

```
http://ваш-ip-адрес
# или
http://ваш-домен.com
# или (если установлен SSL)
https://ваш-домен.com
```

**Первый вход:**
- Username: `admin`
- Password: `admin123` (измените сразу после входа!)

### Шаг 8.3: Тест WebSocket (для barcode scanner)

1. Войдите в систему
2. Перейдите в Daily Picking или Stock-In
3. Переключитесь на вкладку "Телефон" в barcode scanner
4. Проверьте, что статус показывает "🟢 Подключено"

---

## ЧАСТЬ 9: Обслуживание и мониторинг

### Полезные команды PM2

```bash
# Просмотр логов в реальном времени
pm2 logs warehouse

# Просмотр метрик (CPU, Memory)
pm2 monit

# Перезапуск приложения
pm2 restart warehouse

# Остановка приложения
pm2 stop warehouse

# Удалить из списка PM2
pm2 delete warehouse

# Список всех процессов
pm2 list
```

### Обновление приложения

```bash
cd /var/www/warehouse

# Остановить приложение
pm2 stop warehouse

# Обновить код (git pull или загрузить новые файлы)
git pull origin main

# Установить новые зависимости
npm install --production

# Пересобрать приложение
npm run build

# Применить изменения БД (если есть)
npm run db:push

# Запустить приложение
pm2 restart warehouse

# Проверить логи
pm2 logs warehouse --lines 50
```

### Резервное копирование базы данных

```bash
# Создать backup
pg_dump -U warehouse_user -d warehouse_db -h localhost > backup_$(date +%Y%m%d).sql

# Восстановить из backup
psql -U warehouse_user -d warehouse_db -h localhost < backup_20250125.sql
```

Используйте скрипт `deployment/backup.sh` для автоматического резервного копирования.

---

## ЧАСТЬ 10: Решение проблем

### Приложение не запускается

```bash
# Проверить логи PM2
pm2 logs warehouse --lines 100

# Проверить статус
pm2 status

# Попробовать запустить вручную для диагностики
cd /var/www/warehouse
NODE_ENV=production node dist/index.js
```

### Ошибка подключения к БД

```bash
# Проверить статус PostgreSQL
sudo systemctl status postgresql

# Проверить настройки в .env
cat /var/www/warehouse/.env | grep DATABASE_URL

# Тест подключения
psql -U warehouse_user -d warehouse_db -h localhost -W
```

### Nginx 502 Bad Gateway

```bash
# Проверить, что приложение запущено
pm2 status

# Проверить логи Nginx
sudo tail -f /var/log/nginx/warehouse_error.log

# Проверить конфигурацию
sudo nginx -t
```

### WebSocket не работает

```bash
# Проверить логи приложения
pm2 logs warehouse | grep WS

# Проверить конфигурацию Nginx для /ws
sudo nano /etc/nginx/sites-available/warehouse

# Перезапустить Nginx
sudo systemctl restart nginx
```

### Проблемы с производительностью

```bash
# Проверить использование ресурсов
pm2 monit

# Проверить системные ресурсы
htop

# Проверить место на диске
df -h
```

---

## ЧАСТЬ 11: Безопасность

### Изменить пароль администратора

1. Войдите в систему как admin
2. Перейдите в Admin Panel → Users
3. Измените пароль по умолчанию

### Настроить автоматические обновления безопасности

```bash
sudo apt install unattended-upgrades
sudo dpkg-reconfigure -plow unattended-upgrades
```

### Ограничить доступ к PostgreSQL

```bash
# Отредактировать pg_hba.conf
sudo nano /etc/postgresql/14/main/pg_hba.conf

# Убедитесь, что есть строка:
# host    all             all             127.0.0.1/32            md5

# Перезапустить PostgreSQL
sudo systemctl restart postgresql
```

### Настроить регулярные резервные копии

```bash
# Добавить в crontab
crontab -e

# Добавить строку для ежедневного бэкапа в 2 AM:
0 2 * * * /var/www/warehouse/deployment/backup.sh
```

---

## Контрольный список готовности

- [ ] Сервер обновлен (apt update && upgrade)
- [ ] Файрвол настроен (UFW: 80, 443, SSH)
- [ ] Node.js 20+ установлен
- [ ] PostgreSQL установлен и запущен
- [ ] База данных создана
- [ ] Пользователь БД создан
- [ ] Файлы приложения загружены
- [ ] .env файл настроен
- [ ] npm install выполнен
- [ ] npm run build выполнен
- [ ] Схема БД применена (db:push)
- [ ] Администратор создан
- [ ] PM2 установлен
- [ ] Приложение запущено через PM2
- [ ] PM2 автозапуск настроен
- [ ] Nginx установлен
- [ ] Виртуальный хост настроен
- [ ] Nginx перезапущен
- [ ] SSL сертификат установлен (опционально)
- [ ] Приложение доступно в браузере
- [ ] WebSocket работает
- [ ] Пароль администратора изменен

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs warehouse`
2. Проверьте статус сервисов
3. Убедитесь, что все переменные окружения (.env) настроены правильно
4. Проверьте файрвол и сетевые настройки

**Успешного развертывания!** 🚀
